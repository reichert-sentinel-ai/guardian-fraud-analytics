services:
  - type: web
    name: guardian-api
    env: docker
    plan: free
    autoDeploy: true
    dockerfilePath: Dockerfile
    dockerContext: .
    healthCheckPath: /health
    numInstances: 1
    envVars:
      - key: PORT
        value: "8000"

